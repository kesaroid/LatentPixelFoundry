# =============================================================================
# GPU Worker Dependencies
# =============================================================================
# NOTE: This runs on x86_64 CUDA cloud instances, NOT on ARM / Apple Silicon.
#
# ML dependencies (torch, transformers, einops, safetensors, av, etc.) are
# installed via the LTX-2 packages (ltx-core + ltx-pipelines) in the
# Dockerfile. Only worker-specific deps are listed here.
# =============================================================================

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# --- HTTP client (backend callbacks) ---
httpx>=0.28.0

# --- Configuration ---
pydantic>=2.10.0
pydantic-settings>=2.7.0
